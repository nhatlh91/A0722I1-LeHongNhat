var inputAmount, outputAmount, inputCurrency, toCurrency, exchangeRate;
function myFunction() {
    inputAmount = document.getElementById("inputbox").value;
    inputCurrency = document.getElementById("inputcurrency").value;
    toCurrency = document.getElementById("tocurrency").value;
    if(inputCurrency - toCurrency = 0)
        
    outputAmount = inputAmount*23300;
    document.getElementById("result").innerHTML = "Giá trị quy đổi là " + outputAmount + 'VND';
}